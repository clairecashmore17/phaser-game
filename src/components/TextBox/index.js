import React from "react";
import { Navbar, Nav, Container, Modal, Tab } from "react-bootstrap";
const TextBox = (text) => {
  return (
    <>
      <Modal>
        <Modal.Header closeButton>
          <Modal.Title id="signup-modal">Hello</Modal.Title>
        </Modal.Header>
        <Modal.Body></Modal.Body>
      </Modal>
    </>
  );
};

export default TextBox;
